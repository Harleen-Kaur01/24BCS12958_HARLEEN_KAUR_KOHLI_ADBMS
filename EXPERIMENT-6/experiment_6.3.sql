
CREATE TABLE employees (
    EMP_ID NUMBER PRIMARY KEY,
    EMP_NAME VARCHAR2(100),
    EMP_SALARY NUMBER(10,2),
    DEPT_ID NUMBER
);

INSERT INTO employees VALUES (101, 'Rahul', 50000, 10);
INSERT INTO employees VALUES (102, 'Amit', 60000, 10);
INSERT INTO employees VALUES (103, 'Priya', 45000, 20);


SELECT * FROM EMPLOYEES




BEGIN
UPDATE employees
SET EMP_SALARY=EMP_SALARY*1.10
WHERE DEPT_ID=10;

IF SQL%ROWCOUNT>0 THEN
    DBMS_OUTPUT.PUT_LINE('Rows Updated and total count is ' ||
     sql%ROWCOUNT);
ELSE
  DBMS_OUTPUT.PUT_LINE('No row Found');

END IF ;
 END; 
/